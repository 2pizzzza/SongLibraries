basePath: /api
host: localhost:8080
info:
  contact: {}
  description: This is a sample API server.
  title: Example API
  version: "1.0"
paths:
  /hello:
    get:
      description: Returns a greeting message.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: Hello endpoint
swagger: "2.0"
